<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.entregasapp.rapidex">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".SobreActivity" />
        <activity android:name=".MeusPedidosActivity" />
        <activity android:name=".ContatoActivity" />
        <activity android:name=".ServicosActivity" />
        <activity android:name=".RastreioActivity" />
        <activity android:name=".FazerPedidoActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".CadastraseActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>